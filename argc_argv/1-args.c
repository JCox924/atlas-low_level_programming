#include "main.h"
#include <stdio.h>

/**
 * main - prints number of arguments in a program
 *
 * @argc: argument counter
 *
 * Return: always 0
 */

int main(int argc)
{
	printf("%d\n" , argc - 1);

	return (0);
}
